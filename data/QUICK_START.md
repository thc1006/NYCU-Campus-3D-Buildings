# 快速開始 - 數據整理工具使用指南

## 🎯 概述

本工具集提供完整的數據整理、備份和驗證功能，基於 `ymmap_archive/` 的最佳實踐歸檔模式。

## 📋 前置需求

- Docker 和 Docker Compose 已安裝
- 至少 5 GB 可用磁碟空間（用於備份）

## 🚀 使用步驟

### 方法 1: 使用 Docker Compose（推薦）

#### 1. 創建備份

```bash
# 進入 data 目錄
cd "C:\Users\thc1006\Desktop\NQSD\新增資料夾\data"

# 創建備份
docker-compose run --rm backup-creator
```

#### 2. 執行整理

```bash
# 整理所有數據目錄
docker-compose run --rm data-organizer
```

#### 3. 驗證結果

```bash
# 驗證整理後的結構
docker-compose run --rm data-validator
```

### 方法 2: 直接使用 Python 腳本（需要本地 Python 環境）

#### 1. 安裝依賴

```bash
pip install pandas geopandas shapely folium openpyxl rich click
```

#### 2. 執行腳本

```bash
# 創建備份
python scripts/backup_data.py

# 整理數據
python scripts/organize_data.py

# 驗證結果
python scripts/validate_organization.py
```

## 📁 整理後的目錄結構

```
data/
├── raw/                              # 原始數據
│   ├── README.md
│   ├── NLSC_3D_tiles/                # 整理後的 3D Tiles
│   │   ├── metadata.json
│   │   ├── 109_A_yangming/
│   │   ├── 112_A_yangming/
│   │   ├── 112_D_gueiren/
│   │   └── ...
│   └── archive/                      # 舊版本（壓縮）
│
├── processed/                        # 處理後的數據
│   ├── README.md
│   ├── metadata.json
│   ├── buildings/
│   │   ├── by_campus/                # 按校區分類
│   │   ├── combined/                 # 合併數據
│   │   └── osm/                      # OSM 數據
│   └── building_list.txt
│
├── output/                           # 最終輸出
│   ├── README.md
│   ├── latest/                       # 最新版本
│   │   ├── buildings_3d.geojson
│   │   ├── buildings_map.html
│   │   └── ...
│   ├── v1_2026-02-07/                # 版本化存檔
│   └── archive/                      # 舊版本（壓縮）
│
├── floor_plans/                      # 平面圖
│   ├── README.md
│   ├── metadata.json
│   ├── pdf/
│   │   ├── auditorium/
│   │   ├── buildings/
│   │   ├── campus/
│   │   └── administrative/
│   └── preview/
│
├── backup/                           # 備份
│   ├── backup_20260208_123456/
│   └── backup_20260208_123456.tar.gz
│
├── ymmap_archive/                    # 歷史歸檔（只讀，不可修改）
│
└── scripts/                          # 工具腳本
    ├── backup_data.py
    ├── organize_data.py
    └── validate_organization.py
```

## ✅ 驗證檢查項目

整理完成後，驗證工具會檢查：

### raw/ 目錄
- [x] README.md 存在
- [x] NLSC_3D_tiles/ 目錄結構正確
- [x] metadata.json 完整且格式正確

### processed/ 目錄
- [x] README.md 和 metadata.json 存在
- [x] buildings/ 子目錄結構正確
- [x] 文件按校區正確分類

### output/ 目錄
- [x] README.md 存在
- [x] latest/ 包含所有必要文件
- [x] 版本化目錄已建立

### floor_plans/ 目錄
- [x] README.md 和 metadata.json 存在
- [x] PDF 按類型分類
- [x] preview/ 目錄存在

### ymmap_archive/ 目錄
- [x] 目錄完整未被修改
- [x] 僅用於參考，不進行任何操作

## 🔧 故障排除

### 問題 1: Docker 容器無法訪問目錄

**解決方案:**
檢查 `docker-compose.yml` 中的 volumes 掛載路徑是否正確。

### 問題 2: 權限錯誤

**解決方案:**
```bash
# Windows 上確保 Docker Desktop 有權訪問該目錄
# 在 Docker Desktop 設定中添加共享目錄
```

### 問題 3: 驗證失敗

**解決方案:**
查看驗證報告，根據錯誤訊息修復問題，然後重新運行驗證。

## 📊 預期結果

整理完成後，您應該看到：

1. ✅ 所有目錄都有清晰的 README.md
2. ✅ 元數據文件（metadata.json）記錄了數據來源和處理過程
3. ✅ 文件按功能/類型/校區分類清晰
4. ✅ 建立了版本控制系統
5. ✅ 完整的備份已創建
6. ✅ ymmap_archive/ 保持原樣未被修改

## 🎓 最佳實踐

1. **始終先備份**: 在任何整理操作前，先創建完整備份
2. **分階段驗證**: 每整理一個目錄後，運行驗證工具
3. **保留元數據**: 所有處理都應記錄在 metadata.json 中
4. **版本控制**: output/ 目錄應該始終保持版本化
5. **文檔優先**: 每個目錄都應該有清晰的 README.md

## 📝 下一步

整理完成後，建議：

1. 查看各目錄的 README.md 了解數據結構
2. 檢查 metadata.json 確認數據來源和處理參數
3. 壓縮舊版本數據以節省空間
4. 更新主專案文檔，記錄新的目錄結構

## 💡 提示

- 整個流程大約需要 **3-4 小時**
- 備份會佔用約 **650 MB** 空間（未壓縮）
- 壓縮後備份約 **200-300 MB**
- ymmap_archive/ 作為參考標準，**絕不修改**

## 📞 問題回報

如遇到問題，請檢查：
1. `organization_report_*.json` - 整理報告
2. Docker 容器日誌
3. 驗證工具的詳細輸出

---

**最後更新:** 2026-02-08
**工具版本:** 1.0.0
